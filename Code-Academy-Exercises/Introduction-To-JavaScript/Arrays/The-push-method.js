const chores = ['wash dishes', 'do laundry', 'take out trash'];

chores.push('cook dinner', 'mop floor');

console.log(chores);

//  .push() allows us to add items to the end of an array

// why wouldn't we just add this to the original declaration?
